# mdhub go-api environment example
# Bearer token for all API routes. Keep it non-empty in non-local environments.
MDHUB_API_TOKEN=change-me-strong-token

# API listen port (default: 8080)
MDHUB_API_PORT=8080
