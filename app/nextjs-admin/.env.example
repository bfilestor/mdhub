# mdhub nextjs-admin environment example
# Backend API base URL (include protocol)
MDHUB_API_BASE_URL=http://127.0.0.1:8080

# Optional bearer token passed by server-side fetches
MDHUB_API_TOKEN=change-me-strong-token

# Optional app port for `npm run dev`/`npm run start`
PORT=3100
